import LoadingImg from "./loading.gif";

const Loading = () => {
  return (
    <div>
      <img src={LoadingImg} alt="Loading" height="522px" width="475px" />
    </div>
  );
};

export default Loading;
